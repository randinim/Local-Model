v4 plots

- `r2_per_target.png`: horizontal bar chart of per-target R² from model metadata
- `summary.txt`: textual list of per-target R² values

Run `python plot_results.py` to regenerate these files.